<div class="container d-flex mt-4">



    <div class="row">
        {{#each products}}
        <div class="card d-inline col-sm-2 mb-4">
            <a href="/{{this._id}}">
                <img class="card-img-top" src="{{this.image}}" alt="Card image cap">
            </a>
            <div class="d-inline card-body">
                <h5 class="card-title">{{this.price}}</h5>
                <p class="card-text">{{this.name}}</p>
                <a href="#" class="btn btn-primary">Mua hàng</a>
            </div>
        </div>
        {{/each}}
    </div>




</div>